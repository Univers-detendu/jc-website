        {#- block QA ~#}
        <div id="{{ block.name }}" class="flex flex-col md:flex-row space-y-4 md:space-x-4 md:space-y-0">
          {%- for item in block.questions ~%}
          <div class="basis-1/3 p-6 rounded-2xl bg-primary text-white">
            <h3>{{ item.question }}</h3>
            {{ item.answer|markdown_to_html ~}}
          </div>
          {%- endfor ~%}
        </div>